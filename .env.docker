# ChordMe Docker Environment Variables
# This file contains default values for running ChordMe with Docker Compose
# Copy this file to .env.docker and customize if needed

# ===========================================
# DATABASE CONFIGURATION
# ===========================================
POSTGRES_DB=chordme
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password

# ===========================================
# BACKEND CONFIGURATION
# ===========================================
FLASK_ENV=development
FLASK_DEBUG=1
SECRET_KEY=development-secret-key-change-in-production
JWT_SECRET_KEY=development-jwt-secret-key-change-in-production
JWT_EXPIRATION_DELTA=86400
HTTPS_ENFORCED=False

# Database URL (use container service name 'db')
DATABASE_URL=postgresql://postgres:password@db:5432/chordme

# ===========================================
# FRONTEND CONFIGURATION
# ===========================================
# API URL (use container service name 'backend')
VITE_API_BASE_URL=http://localhost:5000
VITE_APP_ENV=development

# ===========================================
# OPTIONAL: REDIS CONFIGURATION
# ===========================================
REDIS_URL=redis://redis:6379/0

# ===========================================
# SECURITY NOTES
# ===========================================
# WARNING: The default passwords above are for development only!
# For production deployment:
# 1. Generate strong random passwords
# 2. Never commit production credentials to version control
# 3. Use environment-specific .env files
# 4. Set HTTPS_ENFORCED=True in production
